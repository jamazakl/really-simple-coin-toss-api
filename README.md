# 🪙 Coin Toss API

A tiny PHP API that simulates flipping a coin and returns the result as JSON.  
You can flip once, or flip many times and get a breakdown of the results.

This project is intentionally simple — no frameworks, no dependencies, just plain PHP.

---

## Why this exists

This project is meant to be:

- A small, easy-to-understand PHP API
- A clean example to upload to GitHub
- A starting point for learning how APIs work
- Something simple you can fork and build on

---

## What it does

- Flips a virtual coin
- Supports one or many tosses
- Returns all outcomes
- Includes a simple summary (heads vs tails)
- Responds in JSON

---

## Requirements

- PHP **7.0 or newer**
- Any web server or PHP’s built-in development server

---

## Running it locally

Clone the repository:

```bash
git clone https://github.com/jamazakl/really-simple-coin-toss-api.git
cd coin-toss-api
